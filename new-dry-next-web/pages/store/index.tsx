import React from "react";
import ComingSoon from "../../components/ComingSoon";

interface StoreProps {}

const Store: React.ComponentType<StoreProps> = (props: StoreProps) => {
  return <ComingSoon />;
};
export default Store;
